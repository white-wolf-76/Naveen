export interface SkillType {
  name: string;
  icon: string;
  percentage: number;
}

export interface ProjectType {
  title: string;
  image: string;
  link: string;
}

export interface SocialLinkType {
  platform: string;
  icon: string;
  link: string;
  hoverColor?: string;
}